<div class="members">
  <header>
    Members

    <%= render partial: "contacts/add_button",
               locals: {
                 sender: current_subject,
                 type: :user
               }
    %>
  </header>

  <%= render current_subject_contacts(current_subject).first(5) %>
  
  <%= link_to 'View more',  contacts_path, :class => "view-all"  %>
</div>
