<% if resource.destroyed? %>
  PermissionCustom.destroy({
    custom: {
      id: "<%= resource.id %>"
    }
  });
<% else %>
  SocialStream.Flash.error('<%= resource.errors.full_messages.to_sentence %>');
<% end %>
