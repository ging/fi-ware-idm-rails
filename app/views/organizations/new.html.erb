<section id="new_organization">
  <% content_for :title do%>
    <%= t('organization.new.action')%>
  <%end%>

  <h3><%= t('organization.new.title') %></h3>

  <%= render partial: 'form',
             object: @organization,
             as: :group %>
</section>
